import Diary from '@/components/screens/Diary';
import UI from '@/components/ui';

const DiaryScreen = () => {
    return (
        <UI.ScreenView>
            <Diary />
        </UI.ScreenView>
    );
};

export default DiaryScreen;
