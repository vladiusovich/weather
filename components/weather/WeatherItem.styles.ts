import { StyleSheet } from 'react-native';
import { colors } from '@/theme/base.styles';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        padding: 15,
        // aspectRatio: 1,
        backgroundColor: colors.background.primary,
        borderRadius: 15,
    },
});

export default styles;
